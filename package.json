{
  "name": "learning2",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean-parcel-dist": "rimraf dist",
    "clean-parcel-cache": "rimraf .parcel-cache",
    "dev": "npm run clean-parcel-dist && npm run clean-parcel-cache && parcel *.html --no-cache --no-source-maps",
    "build": "npm run clean-parcel-dist && npm run clean-parcel-cache && parcel build *.html --public-url /2024-08-30-web-no-fw2/ --no-content-hash --no-source-maps --no-cache",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "assert": "^2.1.0",
    "console-browserify": "^1.2.0",
    "parcel": "^2.12.0",
    "process": "^0.11.10",
    "util": "^0.12.5"
  },
  "dependencies": {
    "rimraf": "^6.0.1"
  }
}
